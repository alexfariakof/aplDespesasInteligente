Problem: Can't resolve 'react/jsx-runtime' error while use try to create the shared component with storybook in react-typescript
RESOLVED:  npm install --save-dev @types/react@latest @types/react-dom@latest


Problem:
interface ILayoutMasterPageProps  {
    titulo: string;
}

export const LayoutMasterPage: React.FC<React.PropsWithChildren> = ({children}) => {
    return (
        <Box>
            Teste LayoutMasterPage-
            {children}
        </Box>
    );
};

Resolved:
import { Box } from "@mui/system";
 
interface ILayoutMasterPageProps  {
    children : React.ReactNode;
    titulo: string;
}

export const LayoutMasterPage: React.FC<ILayoutMasterPageProps> = ({children}) => {
    return (
        <Box>
            Teste LayoutMasterPage-
            {children}
        </Box>
    );
};


Problem: Componentes React DatePiker Day.js

URL: https://mui.com/pt/x/react-date-pickers/getting-started/
   Não resolve a documentção do Material UI  instalar somente npm install @date-io/dayjs

Resolved:
URL-1: https://mui.com/pt/x/react-date-pickers/getting-started/
URL-2: https://www.npmjs.com/package/dayjs
Instalar os 2 pacotes pois precisara do "AdapterDayjs" 
     npm install @date-io/dayjs
     npm install dayjs --save


