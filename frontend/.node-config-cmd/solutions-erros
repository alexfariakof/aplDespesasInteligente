Problem: Can't resolve 'react/jsx-runtime' error while use try to create the shared component with storybook in react-typescript
RESOLVED:  npm install --save-dev @types/react@latest @types/react-dom@latest


Problem:
interface ILayoutMasterPageProps  {
    titulo: string;
}

export const LayoutMasterPage: React.FC<React.PropsWithChildren> = ({children}) => {
    return (
        <Box>
            Teste LayoutMasterPage-
            {children}
        </Box>
    );
};

Resolved:
import { Box } from "@mui/system";
 
interface ILayoutMasterPageProps  {
    children : React.ReactNode;
    titulo: string;
}

export const LayoutMasterPage: React.FC<ILayoutMasterPageProps> = ({children}) => {
    return (
        <Box>
            Teste LayoutMasterPage-
            {children}
        </Box>
    );
};